services:
  - type: web
    name: prompt-plugin
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn prompt_plugin.api:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.12"
    plan: free
